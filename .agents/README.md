# ProxyConfig Agent Assets

This directory is the canonical home for repo-local agent assets.

- `skills/`: Codex/Open Agent Skills for proxy configuration workflows.

Keep directory-specific safety rules close to the files they govern, such as `sing-box/AGENTS.md`.
